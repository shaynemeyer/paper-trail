# Stop Slop Skill

The durable rules are always loaded from `../../steering/stop-slop.md`. Ask Kiro to apply the stop-slop rules when you want them emphasized for a particular task or want existing code or prose revised to follow them. In interactive chat, `/stop-slop` is an optional shortcut.